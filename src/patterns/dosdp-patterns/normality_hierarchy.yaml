pattern_name: normality_hierarchy
pattern_iri: http://purl.archive.org/demkg/patterns/normality_hierarchy.yaml

description: "
  Mirrors the phenotypic abnormality branch to allocate terms for phenotypic normality."

vars:
  sc: "owl:Thing"
  label: "owl:Thing"

name:
  text: "%s"
  vars:
    - label

subClassOf: 
  text: "%s"
  vars:
    - sc

